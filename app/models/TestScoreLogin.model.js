const tsl = (tsl)=>{
this.wheeboxID = tsl.wheeboxID,
this.keySno = tsl.key_sno,
this.compCode = tsl.code,
this.industry = tsl.industry,
this.firstName = tsl.first_name,
this.lastName = tsl.last_name 
}

module.exports = tsl;